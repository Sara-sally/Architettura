Scrivere un metodo estraibit che riceve come parametri due numeri X e Y, e restituisce al chiamante il valore del Y-esimo bit del numero X (assumere che Y sia
compreso tra 0 e 31)
Scrivere anche il main contenente il codice che realizzi la chiamata di tale metodo con parametri attuali rispettivamente 7 e 2 (in notazione 
decimale) e che scriva il risultato restituito dal metodo in una variabile chiamata result (si costruisca  con un ciclo una opportuna maschera da 
mettere in AND con X)


.constant
objref  oxCAFE // may be any value.  Needed by invokevirtual.
.end-constant

.main
  .var
  x
  y
  result
  .end-var
  
  bipush 13
  istore x
  bipush 5
  istore y
  iload x
  iload y
  invokevirtual restoint
  istore result
  iload result 
  halt 
.end-main

.method restoint(x,y)
  .var
  .end-var
  
  loop: iload x       //verifica x<y, se vero esce dal ciclo
        iload y
        isub
        iflt end
        
        iload x
        iload y
        isub
        istore x
        goto loop
        
  end:  iload x
        ireturn
        
.end-method
